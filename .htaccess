## CORS é tratado dinamicamente em backend-sn/connect/cors.php (por origem).
## NÃO adicionar aqui headers Access-Control-Allow-* estáticos — duplicam os
## definidos pelo PHP e o browser rejeita respostas com valores múltiplos.
